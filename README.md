# Milady Line Printer Static Deploy

This is a zero-dependency static build for Vercel.

## Deploy
Upload the contents of this folder to a GitHub repo. There is intentionally no package.json and no node_modules. Vercel should deploy it as a static site with no install/build step.

If Vercel shows framework settings, set Framework Preset to "Other" and leave Install Command and Build Command empty.
